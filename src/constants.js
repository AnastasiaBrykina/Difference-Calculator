const STATUS = {
  added: 'added',
  deleted: 'deleted',
  changed: 'changed',
  unchanged: 'unchanged',
};

const STYLISH_SYMBOLS = {
  added: '+',
  deleted: '-',
  unchanged: ' ',
};

export { STATUS, STYLISH_SYMBOLS };
